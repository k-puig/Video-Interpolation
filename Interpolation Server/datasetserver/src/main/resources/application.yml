spring:
  config:
    import:
      'file:./secrets.yml'
  application:
    name: datasetserver
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    database-platform: 'org.hibernate.dialect.MySQLDialect'
server:
  port: 8080